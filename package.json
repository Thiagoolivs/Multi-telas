{
  "name": "multitelas",
  "version": "1.0.0",
  "private": true,
  "description": "MultiTelas — mídia indoor para TVs corporativas (digital signage)",
  "scripts": {
    "start": "node server.js",
    "build": "npm --prefix web install --include=dev --no-audit --no-fund && npm --prefix web run build",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "fontkit": "^2.0.4",
    "pg": "^8.22.0"
  }
}
